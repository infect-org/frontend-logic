import { observable, computed, action } from 'mobx';

/**
 * Holds all geometric stuff (positions and their calculations, dimensions etc.) of a matrix view.
 */
export default class MatrixViewGeometry {

    dimensions = {
        height: 0,
        width: 0,
    }



}
